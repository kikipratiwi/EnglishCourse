<%= form_for(@student) do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :gender %><br>
    <%= f.text_field :gender %>
  </div>
  <div class="field">
    <%= f.label :birthday %><br>
    <%= f.date_select :birthday, start_year: 1995, end_year: Date.today.year, order: [:day, :month, :year] %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </div>
  <div class="field">
    <%= f.label :level %><br>
    <%= f.collection_select :level_id, Level.all, :id, :name, { :prompt => "Please Select" } %>
  </div>
  <div class="field">
    <%= f.label :room %><br>
    <%= f.collection_select :room_id, Room.all, :id, :name, { :prompt => "Please Select" } %>
  </div>
    <%= f.label :level %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
